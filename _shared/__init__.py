"""Общий слой интеграций securarm / securarm_sensor / securarm_monitor.

**Единственный источник правды** для кода, общего между интеграциями
ARM/ОПС. Файлы этого пакета побайтово копируются в ``<_integration>/_shared``
скриптом ``tools/sync-ha-shared.py``; совпадение копий проверяет
``tools/check-integrations.sh``.

Почему копии, а не прямой импорт между компонентами:
Home Assistant не гарантирует импортируемость одного custom-компонента
из другого, а падение импорта ломает загрузку интеграции. Копия внутри
пакета интеграции делает каждую интеграцию самодостаточной, а общий
источник + sync/check устраняют расхождение версий.

Правьте только канонический пакет ``tools/ha-shared``,
затем запускайте ``tools/sync-ha-shared.py``.
"""

from __future__ import annotations
